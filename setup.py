from setuptools import setup, find_packages

setup(
    name="projetoanalise",  
    version="0.1",          # Versão do pacote
    packages=find_packages(),  # Encontra automaticamente todas as pastas com __init__.py
    install_requires=[],  #Dependências podem ser adicionadas aqui
)
